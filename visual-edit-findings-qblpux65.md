# Verificação da edição visual

- O elemento não localizado pelo editor corresponde à função `ProductWindow` em `client/src/pages/SitePublico.tsx` (linhas 257–288).
- O destaque é renderizado na seção principal da página inicial pela chamada `<ProductWindow />` na linha 306.
- A substituição solicitada deve preservar o espaço, a composição sobre a foto de fundo e a identidade quente do Hubly, trocando apenas o conteúdo visual por uma imagem genérica de agenda, gráficos e controle financeiro.
